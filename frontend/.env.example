# Base URL of the Guessify backend API.
# Leave empty in local dev (Vite proxies /api to localhost:4000).
# In production set this to your deployed backend, e.g. https://api.guessify.app
VITE_API_URL=
